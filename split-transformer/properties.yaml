spec:
  type: object
  properties:
    split:
      type: object
      properties:
        expression:
          title: Expression
          description: The split expression as JsonPath expression.
          type: string
          example: "$.events"
          default: "$"
